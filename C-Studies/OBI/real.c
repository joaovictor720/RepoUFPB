#include <stdio.h>
#include <string.h>

int main(void){
    int i, j, m, n, contDesc, contPart;

    if (scanf("%d", &m) == 1);
    if (scanf("%d", &n) == 1);
    int desc[m], part[n];

    return 0;
}
